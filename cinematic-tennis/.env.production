# In production, the Express backend is served from the same Vercel domain.
# API calls go to /api/* and are routed to the Express server by vercel.json.
# Using empty string means axios calls /api/orders hitting the same domain.
VITE_API_URL=
